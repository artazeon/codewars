function rotateMatrix(matrix) {
  const newMatrix = []
  const row = matrix[0].length
  const col = matrix.length

  for (let i = 0; i < row; i++) {}

  return col
}

console.log(
  rotateMatrix([
    [-1, 4, 5],
    [2, 3, 4],
  ]),
  [
    [5, 4],
    [4, 3],
    [-1, 2],
  ],
)
